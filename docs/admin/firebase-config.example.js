// Copy the Firebase Web app configuration from Firebase Console → Project settings → General → Your apps.
// Values in this file are intentionally placeholders. Firebase Web config is public; never put a
// service-account JSON, private key, GitHub token, or any other privileged credential here.
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_WEB_API_KEY",
  authDomain: "REPLACE_WITH_PROJECT.firebaseapp.com",
  projectId: "REPLACE_WITH_PROJECT_ID",
  appId: "REPLACE_WITH_WEB_APP_ID"
};
