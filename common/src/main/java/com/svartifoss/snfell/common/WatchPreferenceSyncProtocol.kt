package com.svartifoss.snfell.common

/** The same revision accompanies both transports; receivers strip this from user preferences. */
object WatchPreferenceSyncProtocol {
    const val SEQUENCE_KEY = "__svartifoss_preferences_sequence"
}
