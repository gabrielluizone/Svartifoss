<?xml version="1.0" encoding="utf-8"?>
<!-- One subject on a contextual editor page - Progress, Track info, Appearance... - holding every
     setting that is about it. Inflated at bind time, one per group the current face has rows for.
     A card with no heading (the gestures one on Mini buttons, whose single row names itself) hides
     the heading and the gap above the rows; see WatchFacePrefsFragment.inflateEditorGroupCard.

     The card has no horizontal padding of its own: the rows carry theirs, so a row's ripple runs
     the full width of the card instead of stopping a gutter short of its edge. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    app:cardBackgroundColor="@color/lyra_surface"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/lyra_divider"
    app:strokeWidth="1dp">

    <LinearLayout
        android:id="@+id/editor_group_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="12dp"
        android:paddingBottom="2dp">

        <TextView
            android:id="@+id/editor_group_heading"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/google_sans"
            android:includeFontPadding="false"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textAllCaps="true"
            android:textColor="@color/lyra_accent"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="Progress" />

        <LinearLayout
            android:id="@+id/editor_group_rows"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:divider="@drawable/editor_row_divider"
            android:dividerPadding="16dp"
            android:orientation="vertical"
            android:showDividers="middle" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
