<?xml version="1.0" encoding="utf-8"?>
<!-- One swipe-direction/mini-button tile. The caption stays intentionally short (Up, Down,
     Left or Button 1/2/3): it preserves spatial orientation after an action icon replaces the
     directional placeholder, without reintroducing explanatory paragraphs in Controls. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:padding="10dp"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:fontFamily="@font/google_sans"
        android:gravity="center"
        android:ellipsize="end"
        android:importantForAccessibility="no"
        android:maxLines="2"
        android:textColor="@color/lyra_pref_summary"
        android:textSize="11sp"
        android:visibility="visible"
        tools:text="Up" />

</LinearLayout>
