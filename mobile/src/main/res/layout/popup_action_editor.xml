<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minWidth="260dp"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <!-- Icon + name row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/change_icon"
            android:padding="8dp"
            android:scaleType="fitCenter"
            tools:src="@drawable/action_play"
            tools:tint="@android:color/black" />

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/name_box_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/name_box"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/google_sans"
                android:hint="@string/action_text"
                android:inputType="text"
                android:textSize="15sp"
                tools:text="@string/action_play" />

        </com.google.android.material.textfield.TextInputLayout>

    </LinearLayout>

    <!-- Optional config fragment -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/action_config_fragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="16dp"
        tools:name="com.svartifoss.snfell.view.actionconfigs.SkipSecondsConfigFragment" />

    <!-- Swap action button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/swap_button"
        style="@style/LyraGestureButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:text="@string/swap_action" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="12dp"
        android:background="@color/lyra_divider" />

    <!-- Delete / Cancel / OK row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingTop="4dp"
        android:paddingEnd="8dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/delete_button"
            style="?attr/buttonBarButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_weight="1"
            android:fontFamily="@font/google_sans"
            android:gravity="start|center_vertical"
            android:text="@string/delete"
            android:textColor="@color/lyra_accent"
            android:textSize="14sp" />

        <Button
            android:id="@+id/cancel_button"
            style="?attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/google_sans"
            android:text="@android:string/cancel"
            android:textColor="@color/lyra_text_secondary"
            android:textSize="14sp" />

        <!-- Accent applied at runtime (LyraAccent) - the static color here is just the
             pre-resolve fallback, matching the gesture picker's OK. -->
        <Button
            android:id="@+id/ok_button"
            style="?attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/google_sans"
            android:text="@android:string/ok"
            android:textColor="@color/lyra_accent"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>
